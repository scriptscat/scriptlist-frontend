apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "scriptcat-list-frontend.fullname" . }}-test-connection"
  labels:
    {{- include "scriptcat-list-frontend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "scriptcat-list-frontend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
