import { ActionTree } from 'vuex';
import { StateInterface } from '../index';
import { OtherStateInterface } from './state';

const actions: ActionTree<OtherStateInterface, StateInterface> = {
};

export default actions;
